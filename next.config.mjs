/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: true, // You can uncomment this if needed later
  // You can add other Next.js configurations here as your project grows.
};

export default nextConfig;
