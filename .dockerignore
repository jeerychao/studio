
# Git
.git
.gitignore

# Docker
Dockerfile
.dockerignore
docker-compose.yml

# Node
node_modules
npm-debug.log
yarn-error.log

# Next.js
.next
dist
out

# Env files
.env
.env*.local
.env.development
.env.test
# production.env should be managed on the server or via CI/CD, not in the image context directly if it contains secrets
# However, if you have a template or non-sensitive production env, you might adjust.
# For the docker-compose env_file method, this file is on the host.
# For Dockerfile COPY .env method, ensure it's secure.
# Add production.env here if you never want it in any build context.
production.env

# Editor/OS
.vscode
.DS_Store
*.pem
*.key
