# DATABASE_URL="file:./prisma/ipam.db"
# DATABASE_URL="file:./prisma/ipam.db" # Previous attempt, also commenting out
# DATABASE_URL="file:./ipam_main.db" # Previous attempt, also commenting out

# Ensure this file is loaded by Next.js. For Prisma, if DATABASE_URL is not set,
# it will use the 'url' from the datasource block in schema.prisma.
# The schema.prisma should have the hardcoded absolute path:
# url = "file:/home/user/studio/prisma/ipam.db"
