DATABASE_URL="file:/app/prisma/dev.db"
# Ensure NEXT_PUBLIC_ environment variables needed by the client-side are defined here.
# For Next.js server-side code, you can also use variables defined here.
# However, for Docker build-time (like prisma db push), DATABASE_URL should be set
# via ENV in Dockerfile or default in schema.prisma if not overridden.
