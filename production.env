DATABASE_URL="file:/app/prisma/dev.db"
# Add other production-specific environment variables here if needed
# e.g., NEXT_PUBLIC_APP_URL=http://yourdomain.com (if you have one, for client-side use)
# HOSTNAME=0.0.0.0 (Might be needed if Next.js doesn't bind to all interfaces in Docker by default)
